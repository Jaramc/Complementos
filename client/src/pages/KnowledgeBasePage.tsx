import { FileText, Plus, Sparkles } from 'lucide-react';
const articles = [{ id: 1, title: 'Política de reembolsos', content: 'Los reembolsos se procesan dentro de los primeros 15 días hábiles posteriores a la solicitud.', updated: '27 ago 2026' }, { id: 2, title: 'Tiempos de respuesta PQRS', content: 'Cada solicitud recibe una respuesta inicial en un plazo máximo de cinco días hábiles.', updated: '26 ago 2026' }, { id: 3, title: 'Canales de soporte', content: 'Nuestro equipo atiende solicitudes a través del widget web y los canales oficiales.', updated: '24 ago 2026' }];
export function KnowledgeBasePage() { return <section><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-brand-earth">AI knowledge layer</p><h1 className="mt-2 text-3xl font-semibold">Base de conocimiento</h1><p className="mt-2 text-sm text-stone-500">Contenido que responde antes de radicar.</p></div><button className="inline-flex items-center justify-center gap-2 rounded-lg bg-brand-wine px-4 py-2 font-medium text-white transition-colors hover:bg-brand-wine-dark"><Plus size={17} /> Nuevo artículo</button></div><div className="mt-7 grid gap-4 lg:grid-cols-2">{articles.map((article, index) => <article key={article.id} className="rounded-lg border border-brand-earth/15 bg-white p-5"><div className="flex items-start justify-between gap-3"><div className="flex gap-3"><span className="grid size-10 shrink-0 place-items-center rounded-lg bg-brand-light/50 text-brand-wine"><FileText size={19} /></span><div><h2 className="font-semibold">{article.title}</h2><p className="mt-1 text-xs text-stone-400">Actualizado {article.updated}</p></div></div><span className="rounded-full bg-brand-light/40 px-2.5 py-1 text-[11px] font-semibold text-stone-800">Activo</span></div><p className="mt-5 text-sm leading-6 text-stone-600">{article.content}</p><div className="mt-5 border-t border-stone-100 pt-4"><div className="flex items-center justify-between text-xs text-stone-500"><span className="flex items-center gap-1.5"><Sparkles size={14} className="text-brand-olive" /> Embedding vector</span><span className="font-mono text-stone-400">1536 dims</span></div><div className="mt-3 flex h-2 gap-0.5 overflow-hidden rounded-full bg-stone-100">{Array.from({ length: 32 }, (_, bar) => <span key={bar} className={`${(bar + index) % 4 === 0 ? 'bg-brand-earth/45' : 'bg-brand-accent'} flex-1 rounded-sm`} />)}</div></div></article>)}</div></section>; }
