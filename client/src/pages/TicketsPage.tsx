import { Filter, Search, SlidersHorizontal } from 'lucide-react';
import { useState } from 'react';

const tickets = [{ id: 1, code: 'PQRS-8B29D594', subject: 'Cobro duplicado en factura', customer: 'Laura Méndez', type: 'R', priority: 'Alta', sentiment: 'Negativo', status: 'Pendiente' }, { id: 2, code: 'PQRS-7F20A1BC', subject: 'Consulta sobre reembolso', customer: 'Diego Ramírez', type: 'P', priority: 'Media', sentiment: 'Neutral', status: 'En progreso' }, { id: 3, code: 'PQRS-0C13E5A4', subject: 'Excelente atención del equipo', customer: 'Marta Salcedo', type: 'S', priority: 'Baja', sentiment: 'Positivo', status: 'Resuelto' }, { id: 4, code: 'PQRS-45D89B10', subject: 'Entrega fuera del plazo', customer: 'Andrés López', type: 'Q', priority: 'Alta', sentiment: 'Negativo', status: 'Pendiente' }];
const badge = (value: string) => value === 'Alta' || value === 'Negativo' ? 'bg-brand-wine/10 text-brand-wine border border-brand-wine/30' : value === 'Media' || value === 'Neutral' ? 'bg-brand-olive/20 text-stone-900 border border-brand-olive/40' : 'bg-brand-light/40 text-stone-800 border border-brand-accent/40';
export function TicketsPage() { const [search, setSearch] = useState(''); const filtered = tickets.filter((ticket) => `${ticket.subject} ${ticket.customer} ${ticket.code}`.toLowerCase().includes(search.toLowerCase())); return <section><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-brand-earth">Operations queue</p><h1 className="mt-2 text-3xl font-semibold">Tickets</h1><p className="mt-2 text-sm text-stone-500">Monitorea y prioriza las solicitudes de Empresa Demo.</p></div><button className="inline-flex items-center justify-center gap-2 rounded-lg border border-brand-earth/30 px-4 py-2 font-medium text-brand-earth hover:bg-brand-light/30"><Filter size={17} /> Filtros</button></div><div className="mt-7 flex items-center gap-3 rounded-lg border border-brand-earth/15 bg-white px-3 py-2"><Search size={17} className="text-stone-400" /><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Buscar por ticket, cliente o asunto" className="w-full bg-transparent text-sm outline-none" /><SlidersHorizontal size={16} className="text-stone-400" /></div><div className="mt-5 overflow-hidden rounded-lg border border-brand-earth/15 bg-white"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="border-b border-brand-earth/10 bg-brand-surface text-xs uppercase tracking-wider text-stone-500"><tr>{['Ticket', 'Cliente', 'Estado', 'Prioridad', 'Sentimiento'].map((heading) => <th key={heading} className="px-5 py-4 font-semibold">{heading}</th>)}</tr></thead><tbody className="divide-y divide-stone-100">{filtered.map((ticket) => <tr key={ticket.id} className="hover:bg-brand-surface/70"><td className="px-5 py-4"><div className="flex items-center gap-3"><span className="grid size-8 place-items-center rounded-lg bg-brand-light/50 font-bold text-brand-wine">{ticket.type}</span><div><p className="font-mono text-xs text-stone-400">{ticket.code}</p><p className="mt-1 font-semibold">{ticket.subject}</p></div></div></td><td className="px-5 py-4 text-stone-600">{ticket.customer}</td><td className="px-5 py-4"><span className="rounded-full border border-stone-200 px-2.5 py-1 text-xs text-stone-600">{ticket.status}</span></td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-medium ${badge(ticket.priority)}`}>{ticket.priority}</span></td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-medium ${badge(ticket.sentiment)}`}>{ticket.sentiment}</span></td></tr>)}</tbody></table></div></div></section>; }
